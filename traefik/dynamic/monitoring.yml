http:
  routers:
    prometheus:
      rule: "Host(`prometheus.your-domain.ru`)"
      entrypoints:
        - websecure
      middlewares:
        - auth
      service: prometheus
      tls:
        certResolver: myresolver
    
    grafana:
      rule: "Host(`grafana.your-domain.ru`)"
      entrypoints:
        - websecure
      service: grafana
      tls:
        certResolver: myresolver
  
  services:
    prometheus:
      loadBalancer:
        servers:
          - url: "http://prometheus:9090"
    
    grafana:
      loadBalancer:
        servers:
          - url: "http://grafana:3000" 